# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Database | SQLite via aiosqlite | PostgreSQL not installed; zero-setup; single-user local tool; trivially swappable later | Yes — if multi-user deployment needed |
| D002 | M001 | arch | Backend framework | FastAPI + uvicorn | Already in rvc conda env; async support for WebSocket log streaming; minimal overhead | No |
| D003 | M001 | arch | Frontend framework | Next.js 14 App Router + TypeScript + Tailwind | User specified Next.js; App Router is current standard | No |
| D004 | M001 | arch | Audio device routing | sounddevice (Python) loop → BlackHole 2ch output | User confirmed backend-only audio processing; browser only controls start/stop and displays waveforms | No |
| D005 | M001 | arch | Waveform rendering | Web Worker + OffscreenCanvas | Keeps main thread free; explicit user requirement | No |
| D006 | M001 | arch | Realtime inference | rtrvc.py RVC class directly (no copy) | Upstream code is well-maintained; wrapping avoids drift; run from RVC_ROOT working dir | No |
| D007 | M001 | arch | Fine-tuned model storage | Single copy at assets/weights/rvc_finetune_active.pth | User explicitly requested one copy; overwrite on re-train. Base pretrained models are never touched. | Yes — expand to per-profile slots in M002 |
| D008 | M001 | pattern | Audio processor pipeline | List of AudioProcessor objects (process: np.ndarray → np.ndarray) | Explicit user request for future effects extensibility; simple enough to not over-engineer now | Yes — could grow to a graph if needed |
| D009 | M001 | arch | Training subprocess pattern | Launch preprocess.py / extract_feature_print.py / train.py as subprocess, stream stdout via async queue | Matches how web.py orchestrates training; log lines available for WebSocket streaming | No |
| D010 | M001 | constraint | Training device | Try MPS first; fallback to CPU if MPS raises on training ops | MPS available (torch 2.10, mps.is_available=True) but training ops may be unsupported; inference uses MPS | Yes — revisit after S02 testing |
| D011 | M001 | constraint | Service startup | conda run -n rvc uvicorn backend.app.main:app for backend; pnpm dev for frontend | rvc conda env must be active for all Python deps; conda run activates inline | No |
| D012 | M001 | arch | Project structure | rvc-web/ contains backend/ and frontend/ at root alongside Retrieval-based-Voice-Conversion-WebUI/ | Keeps RVC repo self-contained; RVC_ROOT env var bridges backend to RVC code | No |
| D013 | S01 | pattern | DB connection strategy | aiosqlite per-request connections via async context manager (`async with aiosqlite.connect(DB_PATH)`) | Simplest safe pattern for single-user local tool; avoids shared-connection locking complexity | Yes — switch to connection pool if concurrency becomes relevant |
| D014 | S01 | pattern | Test isolation strategy | FastAPI TestClient with temp SQLite DB via env var override; no running server needed for contract tests | httpx TestClient is already installed; per-test temp DB keeps tests hermetic; avoids port conflicts | No |
| D015 | S01 | pattern | Profile ID format | `uuid.uuid4().hex` (32 hex chars, no dashes) | Safe for use as filesystem directory name on all platforms; unambiguous primary key | No |
| D016 | S01 | observability | Startup diagnostics | JSON startup log line + `/health` endpoint expose DB path and RVC_ROOT on every boot | Enables future agents to confirm configuration without reading env files; `/health` is cheap liveness check | No |
| D017 | S02 | pattern | Training job state | In-memory `Dict[str, TrainingJob]` keyed by `profile_id`; single active job at a time | Single-user local tool; no persistence of in-flight state needed; simple and sufficient | Yes — extend to per-job persistence if multi-user or restart-recovery needed |
| D018 | S02 | pattern | Training log streaming — phases 1–3 | `asyncio.create_subprocess_exec` with `stdout=PIPE, stderr=STDOUT` streaming lines into `asyncio.Queue` | Preprocess/extract scripts print to stdout; non-blocking line streaming integrates cleanly with asyncio queue | No |
| D019 | S02 | pattern | Training log streaming — phase 4 (train.py) | Poll `train.log` file via `aiofiles` every 200ms; wait up to 10s for file creation | train.py writes logs only to file via FileHandler — no stdout; file polling is the only viable streaming approach | No |
| D020 | S02 | pattern | FAISS index build | In-process via `run_in_executor(None, _build_index)` adapting `train_index()` logic from `web.py` | CPU-bound; no subprocess needed; keeps index path predictable; avoids spawning another subprocess just for Python logic | No |
| D021 | S02 | pattern | WebSocket contract tests | `starlette.testclient.TestClient.websocket_connect()` for WebSocket tests; `httpx` async client for REST tests | `httpx` does not support WebSocket; starlette TestClient synchronous API works cleanly for WS assertions | No |
| D022 | S02 | constraint | Fixed experiment directory name | `exp_name = "rvc_finetune_active"` — single global training slot; model lands at `assets/weights/rvc_finetune_active.pth` | D007 decision: one fine-tuned copy per D007; fixed name ensures predictable path for S03 to consume | Yes — expand to per-profile in M002 per D007 |
| D023 | S02/T03 | pattern | WebSocket router registration | Separate `ws_router = APIRouter()` (no prefix) for WebSocket endpoint; REST endpoints on `router = APIRouter(prefix='/api/training')` | A router with prefix '/api/training' would make WS route '/api/training/ws/training/{id}' — wrong URL. Two routers, both registered in main.py, produce correct paths | No |
| D024 | S02/T04 | fix | Train subprocess env — rmvpe_root required | Added `rmvpe_root` to `train_env` in `training.py` before launching train subprocess | `save_small_model` in `process_ckpt.py` calls `model_hash_ckpt` → `Pipeline.__init__` which reads `os.environ["rmvpe_root"]`; missing env caused `KeyError` and `.pth` save failure | No |
| D025 | S02/T04 | fix | PyTorch 2.6 weights_only incompatibility in save_small_model | Patched `process_ckpt.py:save_small_model` to catch `model_hash_ckpt` failure and proceed without hash/id metadata | PyTorch 2.6 changed `torch.load` default to `weights_only=True`; fairseq's `load_checkpoint_to_cpu` now raises `UnpicklingError` for HuBERT globals; hash is DRM metadata only — model is fully functional without it | Yes — upstream fairseq fix would be cleaner if maintained |
| D026 | S02/T04 | config | pytest.ini created at repo root | `pytest.ini` at repo root: `asyncio_mode=strict`, `integration` mark registered | No pytest config existed; `PytestUnknownMarkWarning` on `integration` mark; centralises pytest configuration | No |
